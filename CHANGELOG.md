# 更新日志

本文件记录 `astrbot_plugin_life_companion_image` 的重要变更。

## v1.1.7 - 2026-09-04

### 新增

- 增加文生图、自拍和改图的开始/完成日志，记录实际服务商、模型、尺寸、有效提示词、参考图数量、输出大小和耗时；不会记录 API Key。

## v1.1.6 - 2026-09-04

### 修复

- 调整生图顺序：先发送根据上下文生成的自然承接话，再开始图片请求，让用户先得到即时回应。
- 修复 OpenAI/Gitee 兼容服务商收到 `3:4 4K` 等组合尺寸而报“大小必须为 auto 或 WIDTHxHEIGHT”的问题；请求前会转换为合法的精确尺寸。
- 移除 Life Companion 对旧生图插件运行时回退的依赖，避免继续查找旧配置文件；保留 `aiimg_generate` 工具名兼容入口以兼容已有对话调用。
- 修复 LLM 调用 `life_companion_image` 未传参数时，“看看你”等自拍请求误走文生图并使用错误承接话的问题。
- 未指定模式时根据用户原话和消息附图自动选择自拍、改图或文生图；明确指定 `life_photo` 时仍保持文生图行为。
- 增加图片服务商尝试、实际使用和失败回退日志，便于确认切换后的首选服务商及回退原因；不记录 API Key。
- 调整 `/切换生图` 行为：新服务商替换原首选服务商，原首选服务商从链路中移除，不再自动作为备用。

## v1.1.4 - 2026-09-04

### 修复

- 修复 `/切换生图` 新增服务商链路项缺少 `__template_key: provider` 的问题，避免 AstrBot WebUI 显示“未指定模板”。
- 保留原有链路项的完整结构，并为新链路项写入符合 `template_list` 要求的模板标识。

## v1.1.3 - 2026-09-04

### 新增

- 增加 `/生图模型` 命令，列出插件已配置的服务商显示名称、服务商 ID、模型名称和支持的图片类型。
- 增加管理员命令 `/切换生图 服务商名称`，一次切换文生图、自拍和改图的首选服务商。
- 支持 `/切换生图 自拍 服务商名称`、`/切换生图 文生图 服务商名称` 和 `/切换生图 改图 服务商名称` 单独切换功能链路。

### 规则

- 切换命令只接受服务商 `label` 或 `id`；模型名称只用于展示，不作为切换参数。
- 切换服务商时保留原有链路作为失败兜底；目标服务商不支持对应能力时拒绝切换，避免产生部分修改。

## v1.1.2 - 2026-09-04

### 修复

- 生图、生活自拍和改图现在会先启动图片任务，再并行生成一条符合当前人设、上下文和用户语气的自然承接话。
- 移除“正在根据生活状态生成图片，请稍候”等固定、技术化提示。
- 动态承接话超时、模型不可用或输出包含技术词及完成性表述时，自动使用简短自然的操作相关兜底话术。
- 生活伙伴插件的 `今日生活照` 入口优先调用本插件桥接方法，统一使用动态承接话逻辑。

## v1.1.1 - 2026-09-04

### 修复

- 增加旧 Gitee AI Images 工具名 `aiimg_generate` 的兼容入口，避免替换原插件后历史人格或对话仍调用旧工具名而被 AstrBot 跳过。
- 兼容 `text`、`edit`、`selfie_ref` 和 `auto` 模式，并继续使用生活伙伴图片插件当前配置的服务商链路。

## v1.1.0 - 2026-09-04

### 兼容

- 对齐 `astrbot_plugin_gitee_aiimg v5.1.30` 的完整 WebUI 配置结构，保留功能开关、服务商链路、存储、编码、发送、网络、并发、防抖和 providers 设置。
- 支持从 `features.*.chain` 读取服务商，并兼容当前 Gitee 配置中的 Gemini 原生服务商。
- 复制 Gitee AI Images 的现有配置和自拍参考照时，生活插件可以直接读取同一套服务商链路与参考图。

### 修复

- 将生活自拍统一改为由他人从画面外拍摄的自然生活照，不再引导自拍或对镜自拍。
- 移除伸手举手机、对镜看屏幕、手持拍摄设备等动作描述。
- 清理用户提示词、生活状态、时间线、`image_prompt` 和自拍固定前缀中的手机、相机、摄像头、屏幕、自拍杆等拍摄设备描述。
- 保留人物当前活动和咖啡杯等非拍摄类日常物品，避免影响生活场景表达。
- 将单张图片大小上限默认统一为 50 MiB，并读取 Gitee v5 的 `network.max_image_bytes`。
- 增加第三人称拍摄视角和拍摄设备过滤的回归测试。

## v1.0.0 - 2026-09-02

### 新增

- 首次发布独立的 AstrBot Gitee AI Images 生图插件。
- 支持 `/生活照` 文生图，并支持命令末尾追加常用图片比例或尺寸。
- 支持 `/生活自拍`，使用参考人像和 Life Companion 的穿搭、日程、时间线及生活照提示词。
- 支持发送图片后使用 `/生活改图` 修改图片。
- 支持通过 `/生活参考照 设置`、`查看`、`删除` 管理自拍参考照。
- 提供 `life_companion_image` LLM 工具，支持生活照和自拍模式。
- 支持 Gitee API Key 轮询、Base64/URL 图片结果、异步改图任务轮询和本地结果缓存。
- 图片消息发送失败时自动回退为文件发送。
- Life Companion 状态采用只读读取模式，避免准备图片提示词时意外触发新的日程生成。
- 兼容旧版 `astrbot_plugin_life_scheduler` 生活状态接口。

### 测试

- 增加 Gitee 客户端、提示词构造和插件上下文集成测试。
